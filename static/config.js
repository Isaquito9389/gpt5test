// Configuration dynamique pour l'API
window.API_CONFIG = {
    // La clé API sera injectée par le serveur ou configurée manuellement
    API_KEY: "change-me", // À remplacer par votre vraie clé API
    API_URL: window.location.origin + "/scan"
};