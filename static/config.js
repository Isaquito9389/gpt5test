// Configuration dynamique pour l'API
window.API_CONFIG = {
    // La clé API sera définie par le serveur
    API_KEY: "", // La clé sera définie dynamiquement
    API_URL: window.location.origin + "/scan"
};